[build]
builder = "DOCKERFILE"

[deploy]
# /ready reflects real runtime availability: runtime state, subscription,
# all Xray listeners, and Cloudflare Tunnel when Node 5 is enabled.
healthcheckPath = "/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
